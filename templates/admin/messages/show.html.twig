{% extends 'adminbase.html.twig' %}
{% block title %} Kategori Detay {% endblock %}
{% block body %}
    {% block sidebar %} {{ include ('admin/adminsidebar.html.twig') }} {% endblock %}
    {% block content %}
<a style="margin: 0 0 0px 15px" href="{{ path('admin_messages_edit', {'id': message.id}) }}" class="btn btn-large btn-success">Düzenle</a>
<a style="margin-left: 15px" href="{{ path('admin_messages_index') }}" class="btn btn-large btn-warning">Listeye Dön</a>
<button>{{ include('admin/messages/_delete_form.html.twig') }}</button>
<div class="module-body table">
    <div  id="DataTables_Table_0_wrapper" class="dataTables_wrapper" role="grid">
        <table style="margin: 0 0 15px 15px" class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ message.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ message.email }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ message.subject }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.message }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ message.comment }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ message.status }}</td>
            </tr>
        </tbody>
    </table>

        {% endblock %}
        {% endblock %}
