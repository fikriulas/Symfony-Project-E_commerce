{% extends 'adminbase.html.twig' %}
{% block title %} Kategori Detay {% endblock %}
{% block body %}
    {% block sidebar %} {{ include ('admin/adminsidebar.html.twig') }} {% endblock %}
    {% block content %}
<a style="margin: 0 0 0px 15px" href="{{ path('admin_product_edit', {'id': product.id}) }}" class="btn btn-large btn-success">Düzenle</a>
<a style="margin-left: 15px" href="{{ path('admin_product_index') }}" class="btn btn-large btn-warning">Listeye Dön</a>
<button>{{ include('admin/product/_delete_form.html.twig') }}</button>
<div class="module-body table">
    <div  id="DataTables_Table_0_wrapper" class="dataTables_wrapper" role="grid">
        <table style="margin: 0 0 15px 15px" class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ product.title }}</td>
            </tr>
            <tr>
                <th>Keywords</th>
                <td>{{ product.keywords }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ product.type }}</td>
            </tr>
            <tr>
                <th>Publisherid</th>
                <td>{{ product.publisherid }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ product.year }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ product.amount }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Sprice</th>
                <td>{{ product.sprice }}</td>
            </tr>
            <tr>
                <th>Min</th>
                <td>{{ product.min }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ product.detail }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product.image }}</td>
            </tr>
            <tr>
                <th>Writerid</th>
                <td>{{ product.writerid }}</td>
            </tr>
            <tr>
                <th>Categoryid</th>
                <td>{{ product.categoryid }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ product.userid }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ product.status }}</td>
            </tr>
        </tbody>
    </table>


    {% endblock %}
{% endblock %}

