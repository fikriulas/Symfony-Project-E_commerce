{% extends 'base.html.twig' %}

{% block title %}Orderdetails{% endblock %}

{% block body %}
    <h1>Orderdetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orderdetail.id }}</td>
            </tr>
            <tr>
                <th>Orderid</th>
                <td>{{ orderdetail.orderid }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ orderdetail.userid }}</td>
            </tr>
            <tr>
                <th>Productid</th>
                <td>{{ orderdetail.productid }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ orderdetail.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ orderdetail.quantity }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ orderdetail.amount }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ orderdetail.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ orderdetail.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('orderdetails_index') }}">back to list</a>

    <a href="{{ path('orderdetails_edit', {'id': orderdetail.id}) }}">edit</a>

    {{ include('orderdetails/_delete_form.html.twig') }}
{% endblock %}
